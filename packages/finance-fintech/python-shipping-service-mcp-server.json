{
  "type": "mcp-server",
  "packageName": "shipping-service-mcp-server",
  "description": "Provides a Model Context Protocol server for managing shipping orders and address book functionality with support for connecting to real shipping services via HTTP API.",
  "url": "https://github.com/WangTingYeYe/guoguo-mcp-python",
  "runtime": "python",
  "license": "mit",
  "env": {
    "SHIPPING_USE_MOCK": {
      "description": "Whether to use mock data instead of a real API connection",
      "required": true
    },
    "SHIPPING_API_TOKEN": {
      "description": "Access token for the shipping API",
      "required": true
    },
    "SHIPPING_API_BASE_URL": {
      "description": "Base URL for the shipping API",
      "required": true
    }
  }
}