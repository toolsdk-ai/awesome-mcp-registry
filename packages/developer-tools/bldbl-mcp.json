{
	"type": "mcp-server",
	"packageName": "bldbl-mcp",
	"description": "This package enables AI assistants (Claude, GPT, etc.) to work directly with Buildable projects using the Model Context Protocol (MCP). AI assistants can get project context, manage tasks and track progress (for projects created at https://bldbl.dev).",
	"url": "https://github.com/chunkydotdev/bldbl-mcp",
	"runtime": "node",
	"license": "mit",
	"env": {
		"BUILDABLE_API_KEY": {
			"description": "Your Buildable API key (starts with 'bp_')",
			"required": true
		},
		"BUILDABLE_API_URL": {
			"description": "Buildable API URL",
			"required": true
		},
		"BUILDABLE_PROJECT_ID": {
			"description": "Target project ID",
			"required": true
		},
		"BUILDABLE_AI_ASSISTANT_ID": {
			"description": "Unique identifier for your AI assistant",
			"required": true
		}
	},
	"name": "Buildable MCP"
}
